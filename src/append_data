FOLDER=$1
pushd $FOLDER
for h in *_head*.dat; do 
    orig=${h%%_head*}${h#*_head}
    outputfile=${h%%_head*}_append${h#*_head}
    > "$outputfile"
    cat $h >> "$outputfile" 
    cat $orig >> "$outputfile"
done
popd

